[project]
name = "orch-agents"
version = "0.1.0"
requires-python = ">=3.12"

[tool.mypy]
python_version = "3.12"
ignore_missing_imports = true
files = ["_orchestrator", "orch.py"]

[tool.ruff]
exclude = ["__pycache__", ".venv", "features"]
